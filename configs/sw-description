software =
{
    version = "1.0.0";
    hardware-compatibility: [ "mini-linux-aarch64-v1" ];

    rootfs:
    {
        images: (
            {
                filename = "rootfs.squashfs";
                type = "raw";
                device = "inactive-rootfs-slot";
                compressed = false;
            }
        );
    };
}

